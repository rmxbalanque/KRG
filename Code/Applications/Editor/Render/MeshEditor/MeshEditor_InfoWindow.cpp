#include "MeshEditor.h"

//-------------------------------------------------------------------------

namespace KRG::Render
{
    void MeshEditor::DrawMeshInfoWindow()
    {
        if ( ImGui::Begin( "Mesh Info" ) )
        {
            ImGui::Text( "TODO" );
        }
        ImGui::End();
    }
}